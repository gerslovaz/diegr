% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_stats.R
\name{check_structure}
\alias{check_structure}
\title{Check data structure and print inferred hierarchy}
\usage{
check_structure(data, value_col = "signal")
}
\arguments{
\item{data}{A data frame, tibble, or database table containing the EEG data in long format.}

\item{value_col}{Character string specifying the signal amplitude column name (default \code{"signal"}).}
}
\value{
The original data object invisibly, allowing it to be used in pipes.
}
\description{
A diagnostic helper function to run before starting the \code{diegr} analysis.
It infers the experimental hierarchy based on the standardized column names
strictly required by the package and prints a readable summary. Supports
data frames, tibbles, and database tables.
}
\details{
The \code{diegr} package does not strictly require all structural columns for every function
(e.g., \code{group}, \code{condition}, and \code{epoch} may be optional). However, if they are present,
they must follow the exact naming convention (\code{group}, \code{subject}, \code{sensor}, \code{epoch},
\code{condition}, \code{time}). Only the signal amplitude column can be custom-named via the
\code{value_col} argument. This function helps verify which columns were correctly recognized.
}
\examples{
# Checking the structure of epochdata
check_structure(epochdata)
}
